name: flutter_soft_keyboard
description: "This Flutter package is a virtual keyboard widget that can be implemented using a 2D array."
version: 1.1.0
homepage: "https://github.com/aqoong/flutter_soft_keyboard"

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  size_tailored_text: ^1.0.6
  ripple_container: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

license: MIT